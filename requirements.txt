# Pinned to 3.6.9 deliberately: pymodbus 3.14 renamed ModbusSlaveContext ->
# ModbusDeviceContext and removed the old datastore accessors in favour of
# SimData/SimDevice, and 4.0 will make the new API mandatory. Most
# documentation and StackOverflow answers students will find still target
# the pre-4.0 API. Tracked as a known limitation in docs/limitations.md.
pymodbus==3.6.9
pyyaml==6.0.2
requests==2.32.3
# For panel/app.py (the local web control panel), run on the host, not
# in a container — pinned to match hmi/requirements.txt's version.
flask==3.1.0
